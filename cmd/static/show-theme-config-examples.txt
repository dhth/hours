  hours themes show-config                               # shows config for the current theme
  hours themes show-config -t 'default'                  # shows config for the default theme
  hours themes show-config -t 'monokai'                  # shows config for the built-in theme "monokai"
  hours themes show-config -t 'custom:yourtheme'         # shows config for a user-defined theme, with missing elements replaced by those from the default theme
